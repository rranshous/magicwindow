Simple:
  run rranshous/file_receiver - will receive the images from each node
  run rranshous/http_listing - export file receiver container's /data to this container's /data
  use node script on each node in a loop w/ some sleep
